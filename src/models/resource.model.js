/**
 * Model representing the resource entity.
 */

// Example resource model schema
const ResourceSchema = {
  id: String,
  name: String,
  description: String,
  // Add other fields as needed
};

module.exports = ResourceSchema;
